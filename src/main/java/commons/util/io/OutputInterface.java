package commons.util.io;

/**
 * Used to handle output in Duke App.
 */
public class OutputInterface {
    /**
     * Could use OutputInterface.writer() to print.
     */
    public static void writer(String str) {
        System.out.println(str);
    }
}
